[package]
name = "fleet-data"
version = "0.0.1"
edition = "2021"
description = "Typed data loaders and parsers for codex-fleet dashboard state."
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "1"

[dev-dependencies]
insta = "1"
