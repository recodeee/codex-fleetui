[workspace]
resolver = "2"

# Keep the shared input crate explicit because it is the cross-dashboard input
# contract; the remaining fleet-* crates are picked up as their directories land.
members = ["fleet-input", "fleet-*"]
