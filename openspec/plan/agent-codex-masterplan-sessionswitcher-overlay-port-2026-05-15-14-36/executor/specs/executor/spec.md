# Capability Spec: executor

## ADDED Requirements

### Requirement: executor responsibilities for `agent-codex-masterplan-sessionswitcher-overlay-port-2026-05-15-14-36`
This role MUST define and deliver its scoped outputs with evidence.

#### Scenario: Role executes assigned scope
- **WHEN** the role begins execution for `agent-codex-masterplan-sessionswitcher-overlay-port-2026-05-15-14-36`
- **THEN** it follows `tasks.md` and records evidence for completion
