# Capability Spec: executor

## ADDED Requirements

### Requirement: executor responsibilities for `agent-codex-masterplan-ios-bordered-fleet-tab-strip-sub4-2026-05-15-12-41`
This role MUST define and deliver its scoped outputs with evidence.

#### Scenario: Role executes assigned scope
- **WHEN** the role begins execution for `agent-codex-masterplan-ios-bordered-fleet-tab-strip-sub4-2026-05-15-12-41`
- **THEN** it follows `tasks.md` and records evidence for completion
