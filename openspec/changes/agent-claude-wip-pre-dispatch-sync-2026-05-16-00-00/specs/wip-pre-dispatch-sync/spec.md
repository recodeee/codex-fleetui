## ADDED Requirements

### Requirement: wip-pre-dispatch-sync behavior
The system SHALL enforce wip-pre-dispatch-sync behavior as defined by this change.

#### Scenario: Baseline acceptance
- **WHEN** wip-pre-dispatch-sync behavior is exercised
- **THEN** the expected outcome is produced
- **AND** regressions are covered by tests.
