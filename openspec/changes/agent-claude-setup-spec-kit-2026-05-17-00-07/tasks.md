## Definition of Done

- All boxes checked, branch `MERGED` on `origin`, PR URL captured.

## 1. Install

- [x] 1.1 `specify init --here --ai claude --force --ignore-agent-tools` (specify-cli v0.8.11).
- [x] 1.2 Verified `.specify/` + 14 speckit skills + SPECKIT marker on `AGENTS.md`.

## 2. Cleanup

- [x] 2.1 Pruned heavy auto-generated T3 plan workspace (overkill for CLI install).

## 3. Ship

- [ ] 3.1 Commit + push + PR vs `main` + squash auto-merge.
- [ ] 3.2 Record PR URL + `MERGED` state.
- [ ] 3.3 Confirm sandbox worktree pruned.
