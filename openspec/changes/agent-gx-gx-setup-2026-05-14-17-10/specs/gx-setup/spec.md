## ADDED Requirements

### Requirement: gx-setup behavior
The system SHALL enforce gx-setup behavior as defined by this change.

#### Scenario: Baseline acceptance
- **WHEN** gx-setup behavior is exercised
- **THEN** the expected outcome is produced
- **AND** regressions are covered by tests.
